//This file contain function to split cell of html table done
/* This section is provide by codingame, the costum code will be come after that
const NR = parseInt(readline());
for (let i = 0; i < NR; i++) {
    const ROW = readline();
}
var inputs = readline().split(' ');
const IS = parseInt(inputs[0]);
const DS = inputs[1];
*/
